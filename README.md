DNS changer for Windows built with WPF

Allows you to set primary and secondary DNS manually, and choose from default DNS options. Any DNS you add manually is also saved in the default list.

Download & Run: 

Download the latest release ZIP from Releases
Extract the contents to a folder (e.g., C:\DNS-Changer).
Double-click DNS-Changer.exe to run.
